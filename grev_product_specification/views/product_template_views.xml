<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_template_view_form" model="ir.ui.view">
        <field name="name">product_template.view.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view" />
        
        <field name="arch" type="xml">
            <xpath expr="//page[last()]" position="after">
                <page name="specifications" string="Specifications">
                    <field name="specification_ids" nolable="1" >
                        <list editable="bottom" string="Specifications" >
                            <field name="product_tmpl_id" column_invisible="1" />
                            <field name="sequence" widget="handle"/>
                            <field name="name"  required="1"/>
                            <field name="value"  required="1"/>
                            <field name="unit_id" />
                        </list>
                    </field>
                </page>
                
            </xpath>
        </field>
    </record>

</odoo>
